using UnityEngine;

public class ssadsad : MonoBehaviour
{
    public Transform sd;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        transform.rotation = sd.rotation;
    }
}
